# 基于深度学习的跌倒检测系统设计与实现

## 目录

1. [问题描述](#1-问题描述)
   - 1.1 项目背景
   - 1.2 功能需求
   - 1.3 性能要求

2. [方案描述](#2-方案描述)
   - 2.1 技术选型
   - 2.2 架构设计
   - 2.3 核心算法概述

3. [项目设计](#3-项目设计)
   - 3.1 系统架构设计
   - 3.2 模块划分与接口设计
   - 3.3 数据库设计
   - 3.4 关键算法设计

4. [项目实现](#4-项目实现)
   - 4.1 前端实现
   - 4.2 后端实现
   - 4.3 模型实现

5. [项目测试](#5-项目测试)
   - 5.1 测试方案
   - 5.2 测试用例
   - 5.3 测试结果

6. [项目总结](#6-项目总结)
   - 6.1 技术收获
   - 6.2 问题与解决
   - 6.3 改进方向

---

## 1 问题描述

### 1.1 项目背景

随着人口老龄化加剧，老年人居家安全问题日益突出。跌倒是老年人意外伤害的主要原因之一，据统计，65岁以上老年人每年跌倒发生率约为30%。传统的人工监护方式存在成本高、效率低等问题，难以满足大规模养老需求。

本项目旨在开发一套基于深度学习的跌倒检测系统，通过实时视频监控自动识别跌倒行为并及时告警，为老年人居家安全提供技术保障。系统采用前后端分离架构，支持实时监控和离线视频分析两种模式。

### 1.2 功能需求

**用户管理**
- 用户注册、登录、密码管理
- 管理员和普通用户角色划分
- 用户数据隔离，每个用户只能访问自己的数据

**实时监控**
- 支持本地摄像头和RTSP视频流接入
- 实时人体姿态检测和骨骼标注
- 跌倒行为自动识别和告警
- 跌倒时刻自动截图保存
- 检测历史记录查询和展示

**视频分析**
- 视频文件上传（支持MP4、AVI等格式）
- 离线检测模式（快速检测/精确检测）
- 检测结果展示（总帧数、跌倒帧数、告警次数）
- 处理后视频下载

**告警管理**
- 跌倒检测自动生成告警记录
- 告警确认和批量确认功能
- 告警统计和时间范围筛选

**AI助手**
- 集成Ollama大语言模型
- 自然语言交互和问题解答
- 检测数据分析和报告生成

### 1.3 性能要求

- 检测准确率：跌倒检测准确率≥95%
- 实时性：视频流处理帧率≥15fps
- 响应时间：API请求响应时间<500ms
- 并发支持：支持100+用户并发访问
- 存储：支持百万级检测记录存储

---

## 2 方案描述

### 2.1 技术选型

**前端技术**
- Vue.js 3 + Vite 6：前端框架和构建工具
- Element Plus 2：UI组件库
- HTML5 Canvas：视频帧处理和绘图
- Fetch API：HTTP请求

**后端技术**
- Flask 3：Web框架
- MySQL 8：关系型数据库
- Flask-SQLAlchemy：ORM框架
- Flask-CORS：跨域支持

**深度学习技术**
- PyTorch 2：深度学习框架
- MediaPipe Pose：人体姿态估计
- Ollama：大语言模型服务

### 2.2 架构设计

系统采用经典的三层架构：

**表现层**：Vue.js前端应用，负责用户交互和界面展示
**业务逻辑层**：Flask后端服务，处理业务逻辑和API请求
**数据层**：MySQL数据库，存储用户、检测记录、告警等数据

系统核心模块包括：
- 用户认证模块：处理用户注册、登录、权限验证
- 检测模块：实时帧检测和视频分析
- 告警模块：告警生成、确认和统计
- AI助手模块：大语言模型交互和报告生成

### 2.3 核心算法概述

**行为识别算法**
- 使用双向GRU网络（FallActionGRU）进行时序行为分类
- 输入：69维特征向量（33关键点坐标+重心+倾斜角度+宽高比）
- 输出：7种行为类别（空帧、站立、坐着、躺卧、弯腰、爬行、跌倒）

**跌倒检测算法**
- 采用三重判定条件（CGDD+BTD+SCDD）
- CGDD：重心下降检测，阈值0.009m/s
- BTD：身体倾斜检测，阈值45°
- SCDD：轮廓变形检测，阈值1.0

**姿态提取算法**
- 使用MediaPipe Pose提取33个人体关键点
- 关键点包括头部、上肢、下肢等部位
- 计算重心位置、倾斜角度、宽高比等特征

---

## 3 项目设计

### 3.1 系统流程图

#### 3.1.1 实时检测流程图

```
                    ┌─────────────┐
                    │   用户登录   │
                    └──────┬──────┘
                           │
                           ▼
                    ┌─────────────┐
                    │ 进入监控页面 │
                    └──────┬──────┘
                           │
                           ▼
                    ┌─────────────┐
                    │ 启动摄像头   │
                    └──────┬──────┘
                           │
                           ▼
                    ┌─────────────┐
                    │ 视频流显示   │
                    └──────┬──────┘
                           │
                           ▼
              ┌────────────────────────┐
              │ 每100ms截取一帧图像     │
              └───────────┬────────────┘
                          │
                          ▼
              ┌────────────────────────┐
              │ 发送帧图像到后端API     │
              │ POST /api/detect-frame │
              └───────────┬────────────┘
                          │
                          ▼
              ┌────────────────────────┐
              │ 后端执行跌倒检测        │
              │ 1. MediaPipe姿态提取    │
              │ 2. GRU行为识别          │
              │ 3. 三重判定算法         │
              └───────────┬────────────┘
                          │
            ┌─────────────┴─────────────┐
            │                           │
            ▼                           ▼
    ┌───────────────┐          ┌───────────────┐
    │  检测到跌倒    │          │ 未检测到跌倒   │
    └───────┬───────┘          └───────┬───────┘
            │                          │
            ▼                          ▼
    ┌───────────────┐          ┌───────────────┐
    │ 自动截图保存   │          │ 更新状态显示   │
    │ POST /api/    │          │ 显示行为类别   │
    │ save-fall-    │          │ 显示置信度     │
    │ screenshot    │          └───────────────┘
    └───────┬───────┘
            │
            ▼
    ┌───────────────┐
    │ 保存检测记录   │
    │ 到数据库       │
    └───────┬───────┘
            │
            ▼
    ┌───────────────┐
    │ 生成告警记录   │
    │ alert_record  │
    └───────┬───────┘
            │
            ▼
    ┌───────────────┐
    │ 更新检测历史   │
    │ 列表显示       │
    └───────────────┘
```

#### 3.1.2 视频分析流程图

```
    ┌─────────────────┐
    │ 用户选择视频文件 │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ 点击上传按钮     │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ POST /api/upload│
    │ 上传到服务器     │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ 选择分析模式     │
    └────────┬────────┘
             │
     ┌───────┴───────┐
     │               │
     ▼               ▼
┌─────────┐    ┌─────────┐
│快速检测 │    │精确检测 │
│(跳帧)   │    │(逐帧)   │
└────┬────┘    └────┬────┘
     │               │
     ▼               ▼
┌─────────────────────┐
│ POST /api/analyze   │
│ 执行视频分析         │
└────────┬────────────┘
         │
         ▼
┌─────────────────────────────┐
│ FallDetector.detect_video() │
│ 1. 读取视频帧               │
│ 2. 每帧姿态提取             │
│ 3. 行为识别                 │
│ 4. 跌倒检测                 │
│ 5. 生成处理视频             │
└────────┬────────────────────┘
         │
         ▼
┌─────────────────┐
│ 保存检测记录     │
│ detection_record│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 保存告警记录     │
│ alert_record    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 返回分析结果     │
│ - 总帧数         │
│ - 跌倒帧数       │
│ - 告警次数       │
│ - 输出视频路径   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ 前端展示结果     │
│ - 播放处理视频   │
│ - 下载结果       │
└─────────────────┘
```

#### 3.1.3 用户认证流程图

```
    ┌─────────────────┐
    │ 访问登录页面     │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ 输入用户名密码   │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │ 点击登录按钮     │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────────┐
    │ POST /api/auth/login│
    │ 发送登录请求         │
    └────────┬────────────┘
             │
     ┌───────┴───────┐
     │               │
     ▼               ▼
┌─────────┐    ┌─────────┐
│验证成功 │    │验证失败 │
└────┬────┘    └────┬────┘
     │               │
     ▼               ▼
┌─────────────┐ ┌─────────────┐
│保存用户信息 │ │提示错误信息 │
│到Session    │ │"用户名或密码│
│localStorage │ │错误"        │
└──────┬──────┘ └──────┬──────┘
       │               │
       ▼               ▼
┌─────────────┐ ┌─────────────┐
│检查用户角色 │ │返回登录页面 │
└──────┬──────┘ └─────────────┘
       │
 ┌─────┴─────┐
 │           │
 ▼           ▼
┌─────┐   ┌─────┐
│admin│   │user │
└──┬──┘   └──┬──┘
   │         │
   ▼         ▼
┌─────────┐ ┌─────────┐
│管理员   │ │用户首页 │
│仪表盘   │ │UserHome │
│AdminDash│ └─────────┘
└─────────┘
```

#### 3.1.4 跌倒检测算法流程图

```
    ┌─────────────────┐
    │ 输入视频帧图像   │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────────────┐
    │ MediaPipe姿态提取        │
    │ 提取33个人体关键点       │
    └────────┬────────────────┘
             │
             ▼
    ┌─────────────────────────┐
    │ 计算特征向量（69维）     │
    │ - 33点坐标（66维）       │
    │ - 重心y坐标（1维）       │
    │ - 倾斜角度（1维）        │
    │ - 宽高比（1维）          │
    └────────┬────────────────┘
             │
             ▼
    ┌─────────────────────────┐
    │ 更新帧缓存（30帧序列）   │
    └────────┬────────────────┘
             │
             ▼
    ┌─────────────────────────┐
    │ FallActionGRU行为识别    │
    │ 输出7种行为类别概率      │
    └────────┬────────────────┘
             │
             ▼
    ┌─────────────────────────┐
    │ 三重判定跌倒检测         │
    └────────┬────────────────┘
             │
    ┌────────┼────────┐
    │        │        │
    ▼        ▼        ▼
┌──────┐┌──────┐┌──────┐
│ CGDD ││ BTD  ││ SCDD │
│重心  ││身体  ││轮廓  │
│下降  ││倾斜  ││变形  │
│检测  ││检测  ││检测  │
└──┬───┘└──┬───┘└──┬───┘
   │       │       │
   ▼       ▼       ▼
┌──────┐┌──────┐┌──────┐
│速度  ││角度  ││宽高  │
│≥0.009││<45°  ││比>1.0│
│m/s   ││      ││      │
└──┬───┘└──┬───┘└──┬───┘
   │       │       │
   └───────┼───────┘
           │
           ▼
    ┌─────────────────┐
    │ M1 && M2 && M3  │
    │ 三个条件同时满足 │
    └────────┬────────┘
             │
     ┌───────┴───────┐
     │               │
     ▼               ▼
┌─────────┐    ┌─────────┐
│判定跌倒 │    │未判定跌倒│
│is_fall  │    │is_fall  │
│= True   │    │= False  │
└─────────┘    └─────────┘
```

### 3.2 数据库ER图

```
                              ┌────────────────────┐
                              │       user         │
                              ├────────────────────┤
                              │ id (PK)            │
                              │ username           │
                              │ email              │
                              │ password_hash      │
                              │ role               │
                              │ phone              │
                              │ created_at         │
                              │ last_login         │
                              │ is_active          │
                              └─────────┬──────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                   │                   │
                    │ 1:N               │ 1:N               │ 1:N
                    │                   │                   │
                    ▼                   ▼                   ▼
        ┌───────────────────┐ ┌───────────────────┐ ┌───────────────────┐
        │      camera       │ │ detection_record  │ │      alert        │
        ├───────────────────┤ ├───────────────────┤ ├───────────────────┤
        │ id (PK)           │ │ id (PK)           │ │ id (PK)           │
        │ name              │ │ user_id (FK)      │ │ record_id (FK)    │
        │ location          │ │ camera_id (FK)    │ │ user_id (FK)      │
        │ camera_type       │ │ filename          │ │ alert_type        │
        │ url               │ │ file_path         │ │ severity          │
        │ is_active         │ │ output_path       │ │ message           │
        │ user_id (FK)      │ │ screenshot_path   │ │ title             │
        │ status            │ │ total_frames      │ │ confidence        │
        │ resolution        │ │ detected_frames   │ │ acknowledged      │
        │ fps               │ │ fall_detected     │ │ acknowledged_by   │
        └───────────────────┘ │ alert_count       │ │ acknowledged_at   │
                    │         │ status            │ └───────────────────┘
                    │         │ label             │
                    │ 1:N     │ confidence        │
                    │         │ behavior          │
                    ▼         │ detected_at       │
        ┌───────────────────┐ │ created_at        │
        │ detection_record  │ └─────────┬─────────┘
        │ (同上)            │           │
        └───────────────────┘           │ 1:N
                                        │
                                        ▼
                              ┌───────────────────┐
                              │   alert_record    │
                              ├───────────────────┤
                              │ id (PK)           │
                              │ detection_id (FK) │
                              │ user_id (FK)      │
                              │ frame_number      │
                              │ timestamp         │
                              │ behavior          │
                              │ confidence        │
                              │ M1 (CGDD触发)     │
                              │ M2 (BTD触发)      │
                              │ M3 (SCDD触发)     │
                              │ center_gravity_   │
                              │   speed           │
                              │ body_tilt_angle   │
                              │ contour_ratio     │
                              │ acknowledged      │
                              └───────────────────┘
```

**ER图实体关系说明**：

| 实体1 | 关系类型 | 实体2 | 说明 |
|-------|---------|-------|------|
| user | 1:N | camera | 一个用户可拥有多个摄像头 |
| user | 1:N | detection_record | 一个用户可有多条检测记录 |
| user | 1:N | alert | 一个用户可收到多条告警通知 |
| camera | 1:N | detection_record | 一个摄像头可产生多条检测记录 |
| detection_record | 1:N | alert_record | 一条检测记录可产生多条告警详情 |
| detection_record | 1:N | alert | 一条检测记录可产生多条告警通知 |

**数据表详细说明**：

**user表（用户信息表）**
- 存储用户基本信息和认证信息
- role字段区分管理员(admin)和普通用户(user)
- password_hash使用SHA256加密存储

**camera表（摄像头配置表）**
- 存储摄像头设备信息
- camera_type区分本地摄像头(local)和网络摄像头(rtsp)
- status记录摄像头在线/离线状态

**detection_record表（检测记录表）**
- 存储每次检测的完整信息
- screenshot_path保存跌倒截图路径
- fall_detected标记是否检测到跌倒

**alert_record表（告警详情表）**
- 存储每次告警的详细信息
- M1/M2/M3记录三重判定触发状态
- center_gravity_speed等记录具体检测数值

**alert表（告警通知表）**
- 存储告警通知信息
- severity区分告警严重程度
- acknowledged标记告警是否已确认

### 3.3 数据库表结构详细设计

#### 3.3.1 user表（用户信息表）

**表结构**：

| 字段名 | 数据类型 | 长度 | 允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|--------|--------|------|------|
| id | INT | - | 否 | AUTO_INCREMENT | PRIMARY KEY | 用户ID，自增主键 |
| username | VARCHAR | 80 | 否 | - | UNIQUE | 用户名，唯一 |
| email | VARCHAR | 120 | 否 | - | UNIQUE | 邮箱地址，唯一 |
| password_hash | VARCHAR | 512 | 是 | NULL | - | 密码哈希值（SHA256加密） |
| role | VARCHAR | 20 | 否 | 'user' | - | 用户角色：admin/user |
| phone | VARCHAR | 20 | 是 | NULL | - | 手机号码 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | - | 账号创建时间 |
| last_login | DATETIME | - | 是 | NULL | - | 最后登录时间 |
| is_active | BOOLEAN | - | 否 | TRUE | - | 账号是否激活 |

**索引设计**：
- PRIMARY KEY: `id`
- UNIQUE INDEX: `username`
- UNIQUE INDEX: `email`

#### 3.3.2 camera表（摄像头配置表）

**表结构**：

| 字段名 | 数据类型 | 长度 | 允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|--------|--------|------|------|
| id | INT | - | 否 | AUTO_INCREMENT | PRIMARY KEY | 摄像头ID，自增主键 |
| name | VARCHAR | 100 | 否 | - | - | 摄像头名称 |
| location | VARCHAR | 200 | 是 | NULL | - | 安装位置 |
| camera_type | VARCHAR | 20 | 否 | 'rtsp' | - | 类型：local/rtsp |
| url | VARCHAR | 500 | 是 | NULL | - | 视频流地址 |
| is_active | BOOLEAN | - | 否 | TRUE | - | 是否启用 |
| user_id | INT | - | 是 | NULL | FOREIGN KEY | 所属用户ID |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | - | 创建时间 |
| last_check | DATETIME | - | 是 | NULL | - | 最后检查时间 |
| status | VARCHAR | 20 | 否 | 'offline' | - | 状态：online/offline |
| resolution | VARCHAR | 20 | 是 | NULL | - | 分辨率 |
| fps | INT | - | 是 | NULL | - | 帧率 |

**索引设计**：
- PRIMARY KEY: `id`
- FOREIGN KEY: `user_id` REFERENCES `user(id)`
- INDEX: `idx_user_id`

#### 3.3.3 detection_record表（检测记录表）

**表结构**：

| 字段名 | 数据类型 | 长度 | 允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|--------|--------|------|------|
| id | INT | - | 否 | AUTO_INCREMENT | PRIMARY KEY | 记录ID，自增主键 |
| user_id | INT | - | 是 | NULL | FOREIGN KEY | 用户ID |
| camera_id | INT | - | 是 | NULL | FOREIGN KEY | 摄像头ID |
| filename | VARCHAR | 255 | 是 | NULL | - | 原始文件名 |
| file_path | VARCHAR | 500 | 是 | NULL | - | 输入文件路径 |
| output_path | VARCHAR | 500 | 是 | NULL | - | 输出文件路径 |
| screenshot_path | VARCHAR | 500 | 是 | NULL | - | 跌倒截图路径 |
| total_frames | INT | - | 是 | 0 | - | 视频总帧数 |
| detected_frames | INT | - | 是 | 0 | - | 检测帧数 |
| fall_detected | BOOLEAN | - | 否 | FALSE | - | 是否检测到跌倒 |
| alert_count | INT | - | 是 | 0 | - | 告警次数 |
| status | VARCHAR | 20 | 否 | 'pending' | - | 状态：pending/processing/completed |
| label | VARCHAR | 50 | 是 | NULL | - | 检测标签 |
| confidence | FLOAT | - | 是 | NULL | - | 置信度（0-1） |
| behavior | VARCHAR | 50 | 是 | NULL | - | 行为类型 |
| detected_at | DATETIME | - | 是 | NULL | - | 检测时间 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | - | 创建时间 |
| completed_at | DATETIME | - | 是 | NULL | - | 完成时间 |

**索引设计**：
- PRIMARY KEY: `id`
- FOREIGN KEY: `user_id` REFERENCES `user(id)`
- FOREIGN KEY: `camera_id` REFERENCES `camera(id)`
- INDEX: `idx_user_id`
- INDEX: `idx_fall_detected`
- INDEX: `idx_detected_at`

#### 3.3.4 alert_record表（告警详情表）

**表结构**：

| 字段名 | 数据类型 | 长度 | 允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|--------|--------|------|------|
| id | INT | - | 否 | AUTO_INCREMENT | PRIMARY KEY | 告警ID，自增主键 |
| detection_id | INT | - | 是 | NULL | FOREIGN KEY | 检测记录ID |
| user_id | INT | - | 是 | NULL | FOREIGN KEY | 用户ID |
| frame_number | INT | - | 是 | NULL | - | 帧号 |
| timestamp | FLOAT | - | 是 | NULL | - | 时间戳（秒） |
| behavior | VARCHAR | 50 | 是 | NULL | - | 行为类型 |
| confidence | FLOAT | - | 是 | NULL | - | 置信度 |
| M1 | BOOLEAN | - | 否 | FALSE | - | CGDD是否触发 |
| M2 | BOOLEAN | - | 否 | FALSE | - | BTD是否触发 |
| M3 | BOOLEAN | - | 否 | FALSE | - | SCDD是否触发 |
| center_gravity_speed | FLOAT | - | 是 | NULL | - | 重心下降速度（m/s） |
| body_tilt_angle | FLOAT | - | 是 | NULL | - | 身体倾斜角度（度） |
| contour_ratio | FLOAT | - | 是 | NULL | - | 轮廓宽高比 |
| acknowledged | BOOLEAN | - | 否 | FALSE | - | 是否已确认 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | - | 创建时间 |

**索引设计**：
- PRIMARY KEY: `id`
- FOREIGN KEY: `detection_id` REFERENCES `detection_record(id)`
- FOREIGN KEY: `user_id` REFERENCES `user(id)`
- INDEX: `idx_detection_id`
- INDEX: `idx_user_id`

#### 3.3.5 alert表（告警通知表）

**表结构**：

| 字段名 | 数据类型 | 长度 | 允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|--------|--------|------|------|
| id | INT | - | 否 | AUTO_INCREMENT | PRIMARY KEY | 告警ID，自增主键 |
| record_id | INT | - | 是 | NULL | FOREIGN KEY | 检测记录ID |
| user_id | INT | - | 是 | NULL | FOREIGN KEY | 用户ID |
| alert_type | VARCHAR | 50 | 是 | NULL | - | 告警类型 |
| severity | VARCHAR | 20 | 是 | NULL | - | 严重程度：high/medium/low |
| message | TEXT | - | 是 | NULL | - | 告警消息内容 |
| title | VARCHAR | 100 | 是 | NULL | - | 告警标题 |
| confidence | FLOAT | - | 是 | NULL | - | 置信度 |
| acknowledged | BOOLEAN | - | 否 | FALSE | - | 是否已确认 |
| acknowledged_by | INT | - | 是 | NULL | FOREIGN KEY | 确认人ID |
| acknowledged_at | DATETIME | - | 是 | NULL | - | 确认时间 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | - | 创建时间 |

**索引设计**：
- PRIMARY KEY: `id`
- FOREIGN KEY: `record_id` REFERENCES `detection_record(id)`
- FOREIGN KEY: `user_id` REFERENCES `user(id)`
- FOREIGN KEY: `acknowledged_by` REFERENCES `user(id)`
- INDEX: `idx_user_id`
- INDEX: `idx_acknowledged`

#### 3.3.6 conversation_session表（对话会话表）

**表结构**：

| 字段名 | 数据类型 | 长度 | 允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|--------|--------|------|------|
| id | INT | - | 否 | AUTO_INCREMENT | PRIMARY KEY | 会话ID，自增主键 |
| user_id | VARCHAR | 100 | 否 | - | - | 用户ID |
| title | VARCHAR | 200 | 是 | '新对话' | - | 会话标题 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | - | 创建时间 |
| updated_at | DATETIME | - | 是 | NULL | - | 更新时间 |

#### 3.3.7 conversation_history表（对话历史表）

**表结构**：

| 字段名 | 数据类型 | 长度 | 允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|--------|--------|------|------|
| id | INT | - | 否 | AUTO_INCREMENT | PRIMARY KEY | 记录ID，自增主键 |
| session_id | INT | - | 否 | - | FOREIGN KEY | 会话ID |
| user_id | VARCHAR | 100 | 否 | - | - | 用户ID |
| role | VARCHAR | 20 | 否 | - | - | 角色：user/assistant |
| content | TEXT | - | 否 | - | - | 对话内容 |
| timestamp | DATETIME | - | 否 | CURRENT_TIMESTAMP | - | 时间戳 |

#### 3.3.8 analysis_record表（分析记录表）

**表结构**：

| 字段名 | 数据类型 | 长度 | 允许空 | 默认值 | 约束 | 说明 |
|--------|---------|------|--------|--------|------|------|
| id | INT | - | 否 | AUTO_INCREMENT | PRIMARY KEY | 记录ID，自增主键 |
| user_id | INT | - | 否 | - | FOREIGN KEY | 用户ID |
| original_filename | VARCHAR | 255 | 否 | - | - | 原始文件名 |
| processed_filename | VARCHAR | 255 | 是 | NULL | - | 处理后文件名 |
| input_path | VARCHAR | 500 | 否 | - | - | 输入文件路径 |
| output_path | VARCHAR | 500 | 是 | NULL | - | 输出文件路径 |
| file_type | VARCHAR | 20 | 否 | - | - | 文件类型 |
| status | VARCHAR | 20 | 否 | 'processing' | - | 状态 |
| total_frames | INT | - | 否 | 0 | - | 总帧数 |
| detected_frames | INT | - | 否 | 0 | - | 检测帧数 |
| fall_detected | BOOLEAN | - | 否 | FALSE | - | 是否检测到跌倒 |
| alert_count | INT | - | 否 | 0 | - | 告警次数 |
| ai_analysis | TEXT | - | 是 | NULL | - | AI分析结果 |
| created_at | DATETIME | - | 否 | CURRENT_TIMESTAMP | - | 创建时间 |
| completed_at | DATETIME | - | 是 | NULL | - | 完成时间 |

### 3.4 系统架构设计

```
┌─────────────────────────────────────────────────────────────────────┐
│                        前端应用 (Vue.js)                           │
├─────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│  │ 用户端页面  │ │ 管理员页面  │ │ 公共组件    │ │ 路由配置    │   │
│  │ Camera      │ │ Dashboard   │ │ Layout      │ │ Router      │   │
│  │ Upload      │ │ Users       │ │ Login       │ │             │   │
│  │ History     │ │ Cameras     │ │             │ │             │   │
│  │ Alerts      │ │ Statistics  │ │             │ │             │   │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘   │
└───────────────────────────┬─────────────────────────────────────────┘
                            │ HTTP/REST API
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        后端服务 (Flask)                            │
├─────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│  │ 用户认证API │ │ 检测API     │ │ 告警API     │ │ 统计API     │   │
│  │ /api/auth   │ │ /api/detect │ │ /api/alerts │ │ /api/stats  │   │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘   │
│                            │                                       │
│                            ▼                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    业务逻辑层                              │   │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐           │   │
│  │  │ 用户服务    │ │ 检测服务    │ │ AI智能体   │           │   │
│  │  │ UserService │ │ FallDetector│ │ AIAgent    │           │   │
│  │  └─────────────┘ └─────────────┘ └─────────────┘           │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                            │                                       │
│                            ▼                                       │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    数据访问层                              │   │
│  │  ┌─────────────────────────────────────────────────────┐   │   │
│  │  │  SQLAlchemy Models: User, Camera, DetectionRecord   │   │   │
│  │  │  AlertRecord, Alert, ConversationSession            │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────────────┘
                            │ MySQL Protocol
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        MySQL数据库                                │
├─────────────────────────────────────────────────────────────────────┤
│  user | camera | detection_record | alert_record | alert          │
│  conversation_session | conversation_history | analysis_record    │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 模块划分与接口设计

**模块划分**

| 模块 | 职责 | 包含文件 |
|------|------|----------|
| 用户认证 | 用户注册、登录、权限验证 | `/api/auth/*` |
| 实时检测 | 摄像头监控、帧检测 | `/api/detect-frame` |
| 视频分析 | 视频上传、离线分析 | `/api/upload`, `/api/analyze` |
| 告警管理 | 告警生成、确认、统计 | `/api/alerts/*` |
| 数据统计 | 用户、检测、告警统计 | `/api/statistics` |
| AI助手 | 对话、分析、报告 | `/api/ai/*` |

**核心API接口**

| 接口 | 方法 | 功能 |
|------|------|------|
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/register` | POST | 用户注册 |
| `/api/detect-frame` | POST | 实时帧检测 |
| `/api/save-fall-screenshot` | POST | 保存跌倒截图 |
| `/api/detection-history` | GET | 获取检测历史 |
| `/api/upload` | POST | 上传视频文件 |
| `/api/analyze` | POST | 分析视频 |
| `/api/alerts` | GET | 获取告警列表 |
| `/api/alerts/{id}/acknowledge` | POST | 确认告警 |
| `/api/statistics` | GET | 获取统计数据 |

### 3.3 数据库设计

**核心数据表**

**user表**（用户信息）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(80) | UNIQUE, NOT NULL | 用户名 |
| email | VARCHAR(120) | UNIQUE, NOT NULL | 邮箱 |
| password_hash | VARCHAR(512) | | 密码哈希 |
| role | VARCHAR(20) | DEFAULT 'user' | 角色 |
| phone | VARCHAR(20) | | 手机号 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| last_login | DATETIME | | 最后登录时间 |
| is_active | BOOLEAN | DEFAULT TRUE | 是否激活 |

**camera表**（摄像头配置）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 摄像头ID |
| name | VARCHAR(100) | NOT NULL | 摄像头名称 |
| location | VARCHAR(200) | | 安装位置 |
| camera_type | VARCHAR(20) | DEFAULT 'rtsp' | 类型 |
| url | VARCHAR(500) | | 视频流地址 |
| is_active | BOOLEAN | DEFAULT TRUE | 是否启用 |
| user_id | INT | FOREIGN KEY | 所属用户 |
| status | VARCHAR(20) | DEFAULT 'offline' | 状态 |
| resolution | VARCHAR(20) | | 分辨率 |
| fps | INT | | 帧率 |

**detection_record表**（检测记录）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 记录ID |
| user_id | INT | FOREIGN KEY | 用户ID |
| camera_id | INT | FOREIGN KEY | 摄像头ID |
| filename | VARCHAR(255) | | 文件名 |
| file_path | VARCHAR(500) | | 文件路径 |
| output_path | VARCHAR(500) | | 输出路径 |
| screenshot_path | VARCHAR(500) | | 截图路径 |
| total_frames | INT | | 总帧数 |
| detected_frames | INT | | 检测帧数 |
| fall_detected | BOOLEAN | | 是否检测到跌倒 |
| alert_count | INT | | 告警次数 |
| status | VARCHAR(20) | DEFAULT 'pending' | 状态 |
| label | VARCHAR(50) | | 检测标签 |
| confidence | FLOAT | | 置信度 |
| behavior | VARCHAR(50) | | 行为类型 |
| detected_at | DATETIME | | 检测时间 |
| created_at | DATETIME | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

**alert_record表**（告警详情）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 告警ID |
| detection_id | INT | FOREIGN KEY | 检测记录ID |
| user_id | INT | FOREIGN KEY | 用户ID |
| frame_number | INT | | 帧号 |
| timestamp | FLOAT | | 时间戳 |
| behavior | VARCHAR(50) | | 行为类型 |
| confidence | FLOAT | | 置信度 |
| M1 | BOOLEAN | | CGDD触发 |
| M2 | BOOLEAN | | BTD触发 |
| M3 | BOOLEAN | | SCDD触发 |
| center_gravity_speed | FLOAT | | 重心下降速度 |
| body_tilt_angle | FLOAT | | 身体倾斜角度 |
| contour_ratio | FLOAT | | 轮廓宽高比 |
| acknowledged | BOOLEAN | DEFAULT FALSE | 是否已确认 |

### 3.4 关键算法设计

#### 3.4.1 行为识别模型

**模型结构**

行为识别使用FallActionGRU双向GRU网络，结构如下：

- 输入层：69维特征向量
  - 33个关键点坐标（x, y）→ 66维
  - 重心y坐标 → 1维
  - 身体倾斜角度 → 1维
  - 宽高比 → 1维

- GRU层：
  - input_size = 69
  - hidden_size = 128
  - num_layers = 2
  - bidirectional = True（双向）
  - dropout = 0.2

- 分类头：
  - LayerNorm(256)
  - Linear(256 → 128)
  - ReLU
  - Dropout(0.2)
  - Linear(128 → 7)

- 输出：7种行为类别概率

**行为类别定义**

| 索引 | 英文 | 中文 | 说明 |
|------|------|------|------|
| 0 | empty | 空帧 | 无人体 |
| 1 | standing | 站立 | 正常站立 |
| 2 | sitting | 坐着 | 坐姿状态 |
| 3 | lying | 躺卧 | 平躺状态 |
| 4 | bending | 弯腰 | 弯腰动作 |
| 5 | crawling | 爬行 | 爬行动作 |
| 6 | falling | 跌倒 | 跌倒动作 |

#### 3.4.2 跌倒检测算法

**三重判定条件**

跌倒检测采用三重判定条件，三个条件必须同时满足才判定为跌倒：

**条件一：CGDD（重心下降检测）**
- 原理：跌倒时人体重心会快速下降
- 方法：比较当前帧与5帧前的重心位置变化
- 阈值：重心下降速度 ≥ 0.009 m/s

**条件二：BTD（身体倾斜检测）**
- 原理：跌倒后身体会趋于水平
- 方法：计算头部到腿部中心的角度
- 阈值：角度 < 45°

**条件三：SCDD（轮廓变形检测）**
- 原理：跌倒后人体宽高比会增大
- 方法：计算人体边界框的宽高比
- 阈值：宽高比 > 1.0

**检测流程**

```
输入帧 → 提取关节点 → 计算重心位置 → CGDD检测
                      ↓
               计算身体倾斜角度 → BTD检测
                      ↓
               计算边界框宽高比 → SCDD检测
                      ↓
               综合判定（M1 && M2 && M3）
                      ↓
               输出跌倒检测结果
```

#### 3.4.3 关节点模型

系统使用14点关节模型，定义如下：

| 关节点 | 位置 | 说明 |
|--------|------|------|
| head | 头部 | 头部中心点 |
| shoulder_center | 肩膀中心 | 两肩中点 |
| right_shoulder | 右肩 | 右肩关键点 |
| right_elbow | 右肘 | 右肘关键点 |
| right_hand | 右手 | 右手腕关键点 |
| left_shoulder | 左肩 | 左肩关键点 |
| left_elbow | 左肘 | 左肘关键点 |
| left_hand | 左手 | 左手腕关键点 |
| right_hip | 右髋 | 右髋关键点 |
| right_knee | 右膝 | 右膝关键点 |
| right_ankle | 右脚踝 | 右脚踝关键点 |
| left_hip | 左髋 | 左髋关键点 |
| left_knee | 左膝 | 左膝关键点 |
| left_ankle | 左脚踝 | 左脚踝关键点 |

---

## 4 项目实现

### 4.1 前端实现

**实时监控页面**

实时监控页面实现以下功能：
- 摄像头视频流显示
- 实时帧检测和结果展示
- 骨骼标注绘制
- 跌倒截图保存
- 检测历史记录展示

核心流程：
1. 用户点击"启动摄像头"按钮，获取摄像头权限
2. 视频流实时显示在页面上
3. 每隔100ms截取一帧发送到后端检测
4. 后端返回检测结果（行为类型、跌倒判定、三重条件状态）
5. 前端绘制骨骼标注和检测结果
6. 如果检测到跌倒，自动保存截图并添加到历史记录

**视频上传页面**

视频上传页面实现以下功能：
- 文件选择和上传
- 上传进度显示
- 分析模式选择（快速/精确）
- 分析结果展示
- 处理后视频播放和下载

### 4.2 后端实现

**检测API**

```python
@app.route('/api/detect-frame', methods=['POST'])
def detect_frame():
    # 读取上传的帧图像
    file = request.files['frame']
    frame = cv2.imdecode(np.frombuffer(file.read(), np.uint8), cv2.IMREAD_COLOR)
    
    # 获取检测器实例（单例模式）
    detector = get_detector()
    
    # 执行检测
    result = detector.detect_frame(frame, mode='image')
    
    return jsonify(result)
```

**截图保存API**

```python
@app.route('/api/save-fall-screenshot', methods=['POST'])
def save_fall_screenshot():
    # 获取截图文件和用户ID
    file = request.files['screenshot']
    user_id = request.form.get('user_id', type=int)
    
    # 生成文件名（时间戳格式）
    timestamp = datetime.datetime.now().strftime('%Y-%m-%d_%H-%M-%S')
    filename = f"fall_{timestamp}.png"
    
    # 保存到服务器
    screenshot_path = os.path.join(app.config['OUTPUT_FOLDER'], 'fall_screenshots', filename)
    file.save(screenshot_path)
    
    # 保存到数据库
    record = DetectionRecord(
        user_id=user_id,
        filename=filename,
        screenshot_path=screenshot_path,
        fall_detected=True,
        detected_at=datetime.datetime.now()
    )
    db.session.add(record)
    db.session.commit()
    
    return jsonify({'success': True, 'screenshot_path': screenshot_path})
```

**检测历史API**

```python
@app.route('/api/detection-history', methods=['GET'])
def get_detection_history():
    # 获取参数
    user_id = request.args.get('user_id', type=int)
    limit = request.args.get('limit', 20, type=int)
    
    # 查询数据库
    records = DetectionRecord.query.filter(
        DetectionRecord.user_id == user_id,
        DetectionRecord.screenshot_path.isnot(None)
    ).order_by(DetectionRecord.detected_at.desc()).limit(limit).all()
    
    return jsonify({'success': True, 'data': [r.to_dict() for r in records]})
```

### 4.3 模型实现

**FallActionGRU模型**

```python
class FallActionGRU(nn.Module):
    def __init__(self, input_size=69, hidden_size=128, num_layers=2, num_classes=7, dropout=0.2):
        super().__init__()
        self.gru = nn.GRU(
            input_size=input_size,
            hidden_size=hidden_size,
            num_layers=num_layers,
            batch_first=True,
            dropout=dropout if num_layers > 1 else 0.0,
            bidirectional=True,
        )
        self.cls = nn.Sequential(
            nn.LayerNorm(hidden_size * 2),
            nn.Linear(hidden_size * 2, hidden_size),
            nn.ReLU(inplace=True),
            nn.Dropout(dropout),
            nn.Linear(hidden_size, num_classes),
        )

    def forward(self, x):
        out, _ = self.gru(x)
        last = out[:, -1, :]
        return self.cls(last)
```

**FallDetector类**

FallDetector类整合了姿态提取、行为识别和跌倒检测三个模块：

1. 初始化时加载MediaPipe姿态估计器和FallActionGRU模型
2. detect_frame方法处理单帧图像：
   - 使用MediaPipe提取姿态特征
   - 更新帧缓存（用于时序行为识别）
   - 使用FallActionGRU进行行为识别
   - 使用FallDetectionAlgorithm进行跌倒检测
   - 返回综合检测结果

---

## 5 项目测试

### 5.1 测试方案

**测试环境**
- 硬件：Intel i7-10700K CPU, 32GB RAM, NVIDIA RTX 3060
- 软件：Windows 11, Python 3.10, Node.js 20
- 数据库：MySQL 8.0

**测试方法**
- 单元测试：对核心算法模块进行测试
- 集成测试：测试API接口和前后端交互
- 功能测试：测试各个功能模块
- 性能测试：测试系统响应时间和并发能力

### 5.2 测试用例

**用户登录测试**

| 测试用例 | 操作步骤 | 预期结果 | 实际结果 |
|---------|---------|---------|---------|
| 正确登录 | 输入admin/admin123 | 登录成功，跳转到仪表盘 | 通过 |
| 错误密码 | 输入admin/wrong | 提示"用户名或密码错误" | 通过 |
| 空用户名 | 不输入用户名 | 提示"请输入用户名" | 通过 |
| 用户权限 | 普通用户登录 | 只能看到自己的数据 | 通过 |

**实时监控测试**

| 测试用例 | 操作步骤 | 预期结果 | 实际结果 |
|---------|---------|---------|---------|
| 启动摄像头 | 点击启动按钮 | 摄像头正常开启 | 通过 |
| 检测人体 | 站立在摄像头前 | 显示"站立"行为 | 通过 |
| 模拟跌倒 | 做出跌倒动作 | 触发跌倒告警 | 通过 |
| 自动截图 | 检测到跌倒 | 截图保存成功 | 通过 |
| 查看历史 | 查看检测历史 | 显示最近记录 | 通过 |

**跌倒检测准确性测试**

| 测试项 | 测试条件 | 预期结果 | 实际结果 |
|-------|---------|---------|---------|
| CGDD检测 | 重心下降速度0.015m/s | 触发M1 | 通过 |
| BTD检测 | 身体倾斜角度30° | 触发M2 | 通过 |
| SCDD检测 | 宽高比1.5 | 触发M3 | 通过 |
| 综合判定 | 三条件同时满足 | 判定为跌倒 | 通过 |
| 误报测试 | 用户躺下休息 | 不触发跌倒 | 通过 |

**视频分析测试**

| 测试用例 | 操作步骤 | 预期结果 | 实际结果 |
|---------|---------|---------|---------|
| 上传MP4 | 选择MP4文件 | 上传成功 | 通过 |
| 快速检测 | 选择快速模式 | 分析完成 | 通过 |
| 精确检测 | 选择精确模式 | 分析完成 | 通过 |
| 查看结果 | 点击查看 | 显示检测统计 | 通过 |

**API接口测试**

| 接口 | 方法 | 测试条件 | 预期状态码 | 实际结果 |
|------|------|---------|-----------|---------|
| /api/auth/login | POST | 正确凭证 | 200 | 通过 |
| /api/auth/login | POST | 错误凭证 | 401 | 通过 |
| /api/detect-frame | POST | 有效图像 | 200 | 通过 |
| /api/detect-frame | POST | 无效图像 | 400 | 通过 |
| /api/detection-history | GET | 有效用户ID | 200 | 通过 |

### 5.3 测试结果

**功能测试结果**

| 模块 | 测试用例数 | 通过数 | 通过率 |
|------|-----------|-------|--------|
| 用户认证 | 4 | 4 | 100% |
| 实时监控 | 5 | 5 | 100% |
| 跌倒检测 | 5 | 5 | 100% |
| 视频分析 | 4 | 4 | 100% |
| API接口 | 5 | 5 | 100% |

**性能测试结果**

| 测试项 | 结果 |
|--------|------|
| 单帧检测时间 | <200ms |
| API响应时间 | <500ms |
| 视频分析速度 | 约30fps |
| 并发用户支持 | >100 |

---

## 6 项目总结

### 6.1 技术收获

通过本项目开发，团队掌握了以下技术：

1. **深度学习应用**：学会了如何将PyTorch模型部署到实际应用中，包括模型加载、推理优化和集成到Web服务中。

2. **计算机视觉**：掌握了MediaPipe姿态估计技术，学会了从视频流中提取人体关键点并进行特征工程。

3. **全栈开发**：实践了Vue.js + Flask前后端分离架构，学会了RESTful API设计和数据库建模。

4. **系统设计**：掌握了模块化设计方法，学会了如何将复杂系统分解为独立模块，提高代码复用性和可维护性。

### 6.2 问题与解决

**问题1：跌倒检测误报率高**

- 原因：初始方案仅使用宽高比判断跌倒，无法区分主动躺下和意外跌倒
- 解决方案：采用三重判定条件（CGDD+BTD+SCDD），三个条件必须同时满足才判定为跌倒，有效降低了误报率

**问题2：实时检测延迟大**

- 原因：每帧都发送到后端处理，网络延迟累积
- 解决方案：前端进行帧采样，每秒处理10-15帧，平衡性能和检测效果

**问题3：检测历史丢失**

- 原因：初始方案将检测记录存储在前端内存中
- 解决方案：将检测记录存入MySQL数据库，页面加载时从数据库获取历史记录

**问题4：Windows路径处理问题**

- 原因：Windows路径分隔符与URL编码不兼容
- 解决方案：使用正则表达式兼容Windows和Unix路径，提取文件名生成URL

**问题5：用户数据隔离问题**

- 原因：后端API未正确过滤用户数据
- 解决方案：API添加用户ID过滤条件，前端请求添加X-User-Id请求头

### 6.3 改进方向

**功能改进**
- 添加移动端支持，便于用户随时随地查看监控
- 增加多摄像头管理功能，支持同时监控多个位置
- 添加短信/邮件告警通知，及时通知监护人

**性能优化**
- 使用WebSocket替代HTTP请求，减少延迟
- 实现模型量化，提高推理速度
- 添加GPU加速支持，提升检测性能

**算法改进**
- 优化跌倒检测算法参数，提高准确率
- 增加异常行为检测，如剧烈运动、长时间静止等
- 使用更先进的行为识别模型，如Transformer架构

---

## 7 参考文献

[1] Lugaresi C, Tang J, Nash H, et al. MediaPipe: A Framework for Building Perception Pipelines[J]. arXiv preprint arXiv:1907.14622, 2019.

[2] Cho K, Van Merriënboer B, Gulcehre C, et al. Learning Phrase Representations using RNN Encoder-Decoder for Statistical Machine Translation[J]. arXiv preprint arXiv:1406.1078, 2014.

[3] Vue.js官方文档. https://vuejs.org/

[4] Flask官方文档. https://flask.palletsprojects.com/

[5] PyTorch官方文档. https://pytorch.org/docs/

[6] MySQL官方文档. https://dev.mysql.com/doc/